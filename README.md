
Objective
Build a small React app that shows you understand components, props, and useState.

Requirements
Create a React app with Vite.
Build at least three components.
Pass props into at least two components.
Use useState in at least one component.
Add one button or event that changes the page.



Include a short README that explains:
* what the app does

This app is a series of "flashcards" with some study material pertaining to lvl 3 week 1 and 2. 

The user will be able to click a subject which will expand the card and display the study information.

useState is used to track if a "flashcard" is flipped

props are used to pass the data to each Flashcard component, 

* how to install dependencies

tailwindcss is used, install with terminal command:

npm install tailwindcss @tailwindcss/vite


* how to run the app



